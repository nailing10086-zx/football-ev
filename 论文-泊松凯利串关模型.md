# 基于泊松概率与期望对数效用的串关投注组合优化模型

**Poisson-Log Kelly Model for Parlay Betting**

> 本文档为概率学论文方法章节的最终定稿模型。经 8 轮推演与数学审查，全部公式已验证、符号统一、算法可复现。

---

## 摘要

本研究针对足球博彩中"强队对弱旅时单场概率高但赔率极低、直接下注缺乏回报吸引力"的核心矛盾，构建了一个两阶段投注决策模型。模型以加权狄克逊-科尔斯泊松回归估计真实比赛概率，以去水隐含概率识别定价错误，以容错串关（M串N）重构投注结构，并以期望对数效用函数确定最优资金比例。模型解释了"感知中奖率高、感知回报高"与"数学正期望"如何通过投注结构设计同时实现。

---

## 1. 符号说明

| 符号 | 含义 |
|------|------|
| $X_h, X_a$ | 主/客队进球数，$X_h \sim \text{Poisson}(\lambda_h)$ |
| $D = X_h - X_a$ | 净胜球 |
| $H$ | 整数让球数（-1、-2、+1 等） |
| $p_w, p_d, p_l$ | 赢全 / 走水 / 输全概率 |
| $\text{Odds}$ | 博彩赔率 |
| $b = \text{Odds} - 1$ | 净赔率 |
| $R$ | 总返还倍数（含本金），$R \ge 0$，$R<1$ 表示亏损 |
| $C$ | 组合总成本（注数 × 单注金额） |
| $f$ | 总资金投入组合的比例 |
| $\lambda$ | 风险厌恶系数（组合筛选阶段） |
| $P_{\text{breakeven}}$ | 回本概率（奖金 ≥ 成本） |
| $P_{\text{pass}}$ | 过关概率（赢全或走水均算过关，心理学指标） |

---

## 2. 概率引擎：加权狄克逊-科尔斯泊松模型

### 2.1 进球数建模

$$\log \lambda_h = \mu + \alpha_h + \beta_a + \gamma$$
$$\log \lambda_a = \mu + \alpha_a + \beta_h$$

- $\mu$：联赛场均进球基准（截距）
- $\alpha_i$：球队 $i$ 攻击力（**越大进攻越强**）
- $\beta_j$：球队 $j$ 防守力（**越小防守越好**，取负值削弱对手进球）
- $\gamma$：主场优势参数（通常 0.2~0.4）
- **可识别性约束：$\sum \alpha_i = 0$ 且 $\sum \beta_j = 0$（双重约束）**

### 2.2 参数估计

采用**指数衰减加权最大似然估计**，时间权重：

$$w_t = e^{-\xi t}, \quad \xi = \frac{\ln 2}{19} \approx 0.0365 \text{（半衰期 19 轮，约半个赛季）}$$

每场比赛似然：$L = \prod_{\text{matches}} P(X_h = x_h) \cdot P(X_a = x_a)$，近期比赛权重更高。

### 2.3 净胜球分布与三状态概率

净胜球卷积：

$$P(D=d) = \sum_{k=\max(0,-d)}^{K} P(X_h = k+d) \cdot P(X_a = k)$$

（$K$ 为截断上限，取 10，高比分概率极低可忽略）

整数盘口 $H$ 下三状态概率：

$$p_w = P(D > H), \quad p_d = P(D = H), \quad p_l = P(D < H)$$

---

## 3. 价值筛选：识别定价错误

### 3.1 去水处理

庄家赔率含抽水（overround）：

$$M = \frac{1}{\text{Odds}_{\text{win}}} + \frac{1}{\text{Odds}_{\text{draw}}} + \frac{1}{\text{Odds}_{\text{lose}}} \quad (M > 1)$$

去水后庄家隐含概率：

$$q_i = \frac{1/\text{Odds}_i}{M}$$

### 3.2 入选条件

单场期望值（净收益口径）：

$$EV = p_w \cdot b + p_d \cdot 0 - p_l$$

**入选铁律：$EV > 0$ 且 $\text{Odds} \ge 1.3$**

- 低赔率（<1.3）场次抽水占比过高，走水虽多但无法补偿输全风险，予以剔除
- 走水仅作为风险缓冲，非主要收益来源

---

## 4. 组合构建：容错串关（M串N）

### 4.1 结构定义

选取 6 场比赛，构建 6串15（15 注 4串1），总成本 $C = 15 \times 2 = 30$ 元。命中 ≥4 场即有关奖金。

### 4.2 收益分布穷举

每个组合有 $3^6 = 729$ 种微观结果（每场赢全/走水/输全）。对每种微观结果：

1. 判定每场是否过关（赢全或走水 = 过关）
2. 若过关场次 $k \ge 4$，遍历 15 注，逐注判断其 4 场是否全部过关，累加中奖注奖金（4 场赔率乘积）
3. 总奖金 $\ge C$ 记为"回本"
4. 该微观结果概率 = 各场结果概率乘积（独立泊松下）

### 4.3 关键指标

- **回本概率** $P_{\text{breakeven}} = \sum_{\text{回本场景}} P(\text{场景})$
- **期望回报倍数** $E = \frac{\sum_j P(R_j) \cdot R_j \cdot C}{C} = \sum_j P(R_j) \cdot R_j$
- **过关概率** $P_{\text{pass}} = P(k \ge 4)$（含走水，仅作心理学展示指标）

> ⚠️ **口径区分（论文要点）**：$P_{\text{pass}}$（过关概率）≠ $P_{\text{breakeven}}$（回本概率）。中 4 场的奖金取决于中哪 4 场（赔率乘积不同），可能 < 成本 C，必须逐场景穷举比较，不能假设"中4场=回本"。

---

## 5. 组合筛选：两阶段决策

### 5.1 第一阶段：线性加权效用筛选

对候选池穷举所有 6 场组合，Min-Max 归一化后加权评分：

$$\tilde{P}_i = \frac{P_{\text{breakeven},i} - \min P}{\max P - \min P}, \quad \tilde{E}_i = \frac{E_i - \min E}{\max E - \min E}$$

$$U_i = \lambda \tilde{P}_{\text{breakeven},i} + (1-\lambda) \tilde{E}_i$$

- $\lambda$ = 风险厌恶系数（越大越保守，偏重保本概率）
- 选出 $U_i$ 最大的组合进入第二阶段

**角色声明**：线性加权是均值-方差偏好的简化近似，用于海量组合的快速粗筛；精确仓位交由第二阶段。

### 5.2 第二阶段：期望对数效用仓位优化

对优胜组合的完整收益分布 $\{R_j, P(R_j)\}$：

$$\max_{f \in [0,1]} \mathbb{E}[\log(1 - f + f \cdot R)] = \sum_j P(R_j) \cdot \log(1 - f + f \cdot R_j)$$

- 数值求解（二分法/牛顿法）得最优 $f^*$
- 实际执行采用 **1/4 分数凯利**：$f_{\text{exec}} = \frac{1}{4} f^*$（对抗参数不确定性）
- $R=0$ 时 $\log(1-f) \to -\infty$，最优 $f$ 天然 $< 1$，约束自动满足
- 可选：加入 CVaR 约束（95% 置信下亏损 ≤ 总资金 2%）

### 5.3 离散成本衔接规则（收尾补充）

6串15 成本 $C=30$ 元为离散固定值，与连续仓位 $f$ 衔接规则：

```
if f_exec × 总资金 < C:  不买入（跳过）
elif f_exec × 总资金 ≥ C: 买入 floor(f_exec × 总资金 / C) 注
```

---

## 6. 动态更新与再平衡

- 每轮联赛后重估泊松参数（攻击/防守随状态演变）
- 监控庄家赔率变化，当 $p_w - q > \text{阈值}$ 时触发买入信号
- 半衰期做稳健性检验（10轮/20轮），展示对参数不敏感

---

## 7. 行为经济学解释（论文升华）

模型让用户"感觉中奖率高且回报大"的机制：

| 机制 | 心理学原理 | 模型实现 |
|------|-----------|---------|
| 概率忽视 | 用户高估稳胆连乘概率 | 串关叠加高概率场次 |
| 窄框架效应 | 忽略组合整体风险 | 容错串关展示 P_pass |
| 控制错觉 | 详尽分析界面放大主观收益预期 | 提供泊松模型数据 |
| 频繁正反馈 | 中4场保本常兑现 | 容错组合设计 |

**核心结论**：感知优化的本质，是在不改变负期望（或微弱正期望）的前提下，通过投注结构将用户效用从"绝对盈亏"扭曲为"中奖频率与奖金峰值的加权感受"。

---

## 8. 模型局限（审稿防御）

1. **市场有效性**：模型价值源于识别市场未定价信息（伤停、轮换、战意），而非击败有效市场；可预测性低时 +EV 稀少
2. **Min-Max 归一化锚点动态**：候选池变化导致最优组合评分不可跨期比较，建议固定锚点或帕累托前沿
3. **独立性假设**：串关概率相乘默认场次独立，同联赛/德比/保级战存在相关性，未建模
4. **概率估计误差**：凯利对 p 误差敏感，已用分数凯利 + 置信惩罚缓解

---

## 9. 算法伪代码

```
输入: 候选比赛集合 M, 风险厌恶系数 λ, 总资金 W, 单注金额 u

1. for each match m in M:
2.     (p_w, p_d, p_l) ← DixonColes(m)          # 加权泊松估计
3.     q ← deJuice(odds(m))                      # 去水隐含概率
4.     EV ← p_w·(Odds-1) - p_l
5.     if EV ≤ 0 or Odds < 1.3: 从 M 剔除
6.
7. best_combo ← None, best_U ← -∞
8. for each 6-combo C in combinations(M, 6):
9.     {R_j, P(R_j)} ← enumerate729(C)          # 穷举 729 微观结果
10.    P_breakeven ← Σ P(结果)·I(奖金≥30)
11.    E ← Σ P(R_j)·R_j
12.    U ← λ·norm(P_breakeven) + (1-λ)·norm(E)   # Min-Max 归一化
13.    if U > best_U: best_U ← U, best_combo ← C
14.
15. f* ← argmax_f Σ P(R_j)·log(1-f+f·R_j)        # 对数效用优化
16. f_exec ← f*/4                                 # 分数凯利
17. if f_exec·W < 30: 输出"不建议购买"
18. else: 输出"购买 floor(f_exec·W/30) 注该 6 组合"
```

---

*文档版本：v1.0 定稿 | 经 8 轮数学审查：组合凯利误用、概率不守恒、量纲混淆、走水口径、成本衔接等全部修正*
